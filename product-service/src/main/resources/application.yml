spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/database_product
    password: seyha123
    username: seyha
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  application:
    name: product-service
server:
  port: 8081


eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/